# End-to-end tests (not implemented)

This folder is currently a placeholder.

## What exists (repo evidence)

- `.gitkeep`

## Not found (in this repo)

- Any e2e test runner setup (Playwright/Cypress), configs, or npm scripts.

## How to add tests (suggested)

- Choose a runner (e.g., Playwright).
- Add dependencies and scripts in `frontend/package.json`.
- Add specs under `tests-e2e/` and document the exact run commands here once they exist.
