# AI workspace (scaffold)

This folder is reserved for analytics / ML work.

## What exists (repo evidence)

- `notebooks/` (placeholder: `.gitkeep`)
- `service/` (placeholder: `.gitkeep`)

## Not found (in this repo)

- Any notebooks, training code, or an inference service implementation.
- Any Dockerfile under `ai/`.

## How to add AI code here (suggested)

- Put exploratory notebooks in `ai/notebooks/`.
- Put a small inference API (if needed) in `ai/service/` and document its run commands here once implemented.
